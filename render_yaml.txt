services:
  - type: web
    name: pronunciation-assessment-mvp
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SPEECHSUPER_APP_KEY
        sync: false
      - key: SPEECHSUPER_SECRET_KEY
        sync: false
    plan: free